External Database (extended) authentication method

This method replicates the existing external database method in moodle but
also adds fields for prefix and postfix salt to be added to the user's
password before it is hashed.
